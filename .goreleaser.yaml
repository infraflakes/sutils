# .goreleaser.yaml
project_name: sutils
version: 2
builds:
  - id: sutils
    main: ./
    binary: sn
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
    goarch:
      - amd64

checksum:
  name_template: 'checksums.txt'

release:
  github:
    owner: infraflakes
    name: sutils
